/**
 * Load Dependencies
 */

const { createEmployee, bulkCreateEmployee } = require('./employee');

/**
 * Expose to use in other files
 */
module.exports = {
  createEmployee,
  bulkCreateEmployee,
};
